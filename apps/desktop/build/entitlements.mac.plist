<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Required for Electron's V8 JIT under Hardened Runtime (also required for notarization). -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <!--
    Electron ships frameworks signed by a different Team ID. Without this,
    ad-hoc-signed builds are killed on arm64 ("code has no resources...").
    Also commonly required for Electron + native modules under Developer ID.
  -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
